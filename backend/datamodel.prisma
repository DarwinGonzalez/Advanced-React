type User {
  id: ID! @id @unique
  name: String!
  email: String!
}

type Item {
  id: ID! @id @unique
  title: String!
  description: String!
  image: String!
  largeImage: String!
  price: Int!
  # createAt: DateTime!
  # updatedAt: DateTime! @updatedAt
  # user: User!
}